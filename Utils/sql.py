# ブランド名（重複なし）
SELECT_DISTINCT_BRAND_NAME = "SELECT DISTINCT BRAND_NAME FROM RIP_MASTER WHERE HIDDEN_FLAG != '1';"

# リップ情報取得（リップID)
SELECT_WHERE_RIP_ID = "SELECT * FROM RIP_MASTER WHERE LIP_ID = %(lipId)s AND HIDDEN_FLAG != '1';"

# リップ情報取得（ブランド名)
SELECT_WHERE_BRAND_NAME = "SELECT * FROM RIP_MASTER WHERE BRAND_NAME = %(brandName)s AND HIDDEN_FLAG != '1';"

# リップ情報取得（ブランド名＆カラーコード）
SELECT_WHERE_BRAND_COLORCODE = "SELECT * FROM RIP_MASTER WHERE BRAND_NAME = %(brandName)s AND COLORCODE = %(colorCode)s AND HIDDEN_FLAG != '1';"

# リップ情報取得（同一色）
SELECT_WHERE_COLORCODE = "SELECT * FROM RIP_MASTER WHERE COLORCODE = %(colorCode)s AND HIDDEN_FLAG != '1';"

# リップ情報取得（類似色）
SELECT_WHERE_SIMILAR_COLORCODE = "SELECT * FROM RIP_MASTER WHERE (COLORCODE >= %(downVCode)s AND COLORCODE <= %(upVCode)s) OR (COLORCODE >= %(downSCode)s AND COLORCODE <= %(upSCode)s) AND HIDDEN_FLAG != '1';"